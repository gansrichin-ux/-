import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const background = Color(0xFF081018);
  static const surface = Color(0xFF101922);
  static const surfaceElevated = Color(0xFF15222D);
  static const surfaceMuted = Color(0xFF1A2732);

  static const border = Color(0xFF2A3A48);
  static const borderMuted = Color(0xFF22303B);

  static const textPrimary = Color(0xFFF4F7FB);
  static const textSecondary = Color(0xFFB7C3D0);
  static const textMuted = Color(0xFF748394);

  static const primary = Color(0xFF4F8DFF);
  static const primaryHover = Color(0xFF73A6FF);
  static const primaryMuted = Color(0xFF163861);

  static const success = Color(0xFF25C16F);
  static const warning = Color(0xFFF5B841);
  static const danger = Color(0xFFFF5A5F);
  static const info = Color(0xFF36C5F0);

  static const statusDraft = Color(0xFF8B96A8);
  static const statusPublished = Color(0xFF4F8DFF);
  static const statusActive = Color(0xFF7C5CFF);
  static const statusTransit = Color(0xFF36C5F0);
  static const statusDelivered = Color(0xFF25C16F);
  static const statusClosed = Color(0xFF16A394);
  static const statusCancelled = Color(0xFFFF5A5F);
  static const statusDispute = Color(0xFFE56BFF);

  static const lightBackground = Color(0xFFF4F7FA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceElevated = Color(0xFFF8FAFC);
  static const lightBorder = Color(0xFFD7DFEA);
  static const lightTextPrimary = Color(0xFF111827);
  static const lightTextSecondary = Color(0xFF475569);
  static const lightTextMuted = Color(0xFF718096);
}
